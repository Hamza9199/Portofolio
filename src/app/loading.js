import Loading from "@/components/Loading";

export default function AppLoading() {
  return (
    <Loading
      visible
      headline="Lights out"
      subline="Deploying the next lap"
    />
  );
}
